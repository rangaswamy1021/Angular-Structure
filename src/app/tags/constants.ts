export enum TagRequestType {
    All,
    New,
    Additional,
    Replace
}

export enum TagStatus {
    Assigned,
    Replaced,
    Expired,
    Lost,
    Stolen
}

export enum TagLocations{
    Customer
}

export enum ActivityType{
    TAGASGN
}