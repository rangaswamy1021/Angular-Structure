//import { IPaging } from '../../shared/contracts/paging';

export interface ITagConfigurationResponse {
    ProtocolId: number,
    MountingId:number,
    TagDeposit: number,
    TagFee: number,
    Protocol: string,
    Mounting:string,
    Tagcount:number     
}