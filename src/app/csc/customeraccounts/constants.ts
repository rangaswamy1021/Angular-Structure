export enum NameType {
    Primary,
    Secondary
}

export enum ReplenishmentType {
    CreditCard,
    ACH,
    Cash
}


export enum AdjustmentApprovedStatus {
    Verified,
    Variance,
    Reopen,
    Reconciled,
    Pending,
    Rejected,
    Approved,
}

export enum TollType {
    PREPAID,
    POSTPAID
}
export enum AnonymousPaymentStatus{
    Pending
}
